In order to run those apps through terminal follow next steps:

1) Open your terminal in the same folder where you find the class you want to run

2) Compile a Java file to generate a class:
  javac filename.java

3) Execute the generated class:
  java filename